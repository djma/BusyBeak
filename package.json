{
  "name": "the-receipts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src/*/index.ts --bundle --outdir=dist --platform=browser",
    "watch": "esbuild src/*/index.ts --bundle --outdir=dist --platform=browser --watch"
  },
  "author": "",
  "license": "UNLICENSED",
  "repository": "https://github.com/djma/the-receipts",
  "bugs": "https://github.com/djma/the-receipts/issues",
  "homepage": "https://github.com/djma/the-receipts#readme",
  "devDependencies": {
    "esbuild": "^0.16.14",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "webextension-polyfill-ts": "^0.26.0"
  }
}
